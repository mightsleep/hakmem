rd_("DgThe <code>x</code> coordinate, still dilated (for arithmetic in place).BmThe <code>y</code> coordinate, still dilated.kBitwise OR.oOnly bit 0 set.lBitwise AND.Ck<code>true</code> when bit <code>i &lt; BITS</code> is set.lBitwise NOT.ClSet view: a word as a set of positions <code>0..BITS</code>.BoLogical shift left by <code>n &lt; BITS</code>.C`Logical shift right by <code>n &lt; BITS</code>.lBitwise XOR.nWidth in bits.C`Every combinator of the algebra as a method. \xe2\x80\xa6mAll bits set.Ed<code>N</code> little-endian limbs of 64 bits: limb 0 holds bits <code>0..64</code>.DfA fixed-width word of <code>BITS</code> bits, bit 0 least significant.oAll bits clear.CgThe one trait to import: every combinator, on every \xe2\x80\xa6AlThe raw dilated bit pattern.mThe raw code.Dh<code>self - 1</code> in dilated space, wrapping: the borrow tunnels \xe2\x80\xa6AoReturns the argument unchanged.0000CkTwo-dimensional runs: rectangular blocks in a bitmap of \xe2\x80\xa6Ee<code>self + 1</code> in dilated space, wrapping at <code>width</code> bits: fill \xe2\x80\xa6BaCalls <code>U::from(self)</code>.0000CdThe algebra\xe2\x80\x99s laws as exported property functions.DfThe positions this dilation occupies: bits <code>0, D, 2D, \xe2\x80\xa6</code>.BkPDEP by walking every position of the word.CaParallel bit deposit (PDEP): scatters the low \xe2\x80\xa6BkPEXT by walking every position of the word.DiParallel bit extract (PEXT): gathers the bits of <code>self</code> at \xe2\x80\xa6EjNumber of set bits at positions <code>&lt; i</code>. <code>i</code> may equal the slice\xe2\x80\xa6DhA multi-limb carrier: <code>N \xc3\x97 64</code> bits that behave as one word.ClThe carrier: a fixed-width word viewed as a container of \xe2\x80\xa6FcBits of <code>other</code> where <code>mask</code> is set, bits of <code>self</code> elsewhere, \xe2\x80\xa6BcThe limbs, least significant first.CfBit-parallel edit distance (Myers 1999, Hyyr\xc3\xb6\xe2\x80\x99s \xe2\x80\xa6ClBit-set operations over a slice of words: the index-free \xe2\x80\xa6CmNumber of integer bits that fit: <code>ceil(BITS / D)</code>.BdIterator behind <code>search</code>.EfAn 8\xc3\x978 bit matrix in a <code>u64</code>: bit <code>8\xc2\xb7row + col</code>. For chess \xe2\x80\xa6BnSplits the code back into <code>(x, y)</code>.EeInterleaves <code>x</code> (even bits) and <code>y</code> (odd bits). Coordinates \xe2\x80\xa6EgScatters the low <code>count_ones(mask)</code> bits of <code>self</code> to the set \xe2\x80\xa6fhakmemCd<code>true</code> when an odd number of bits is set.DeApproximate occurrences of <code>pattern</code> in <code>text</code>.EgPosition of the <code>k</code>-th set bit (0-indexed), if <code>k &lt; popcount</code>.0CgPosition of the <code>k</code>-th set bit, by counting.BnThe code of <code>(x + 1, y)</code>, wrapping.BnThe code of <code>(x, y + 1)</code>, wrapping.BgReplaces the <code>x</code> coordinate.BgReplaces the <code>y</code> coordinate.EdAn integer whose bits sit at positions <code>0, D, 2D, \xe2\x80\xa6</code> of <code>W</code>.EeA 2D Morton code: <code>x</code> dilated by 2 in the even bits, <code>y</code> in \xe2\x80\xa6EhGathers the bits of <code>self</code> at the set positions of <code>mask</code> into \xe2\x80\xa6BlDilated integers and Morton (Z-order) codes.DhCells reachable from the set bits of <code>self</code> by repeatedly \xe2\x80\xa6Em<code>true</code> iff <code>self</code> contains at least one run of <code>k</code> set bits.Bj<code>true</code> for exactly one set bit.Be<code>true</code> when no bit is set.BdPermutations built from delta swaps.EbEverything most code needs: the <code>Bits</code> trait, the <code>Word</code> \xe2\x80\xa6BoHigh bit of every lane equal to <code>b</code>.FhHigh bit of every lane whose byte is <code>&gt;= n</code>, for <code>n</code> in <code>1..=128</code>\xe2\x80\xa6FeHigh bit of every lane whose byte is <code>&lt; n</code>, for <code>n</code> in <code>1..=128</code>.CmHow the shipped kernels were composed, so you can compose \xe2\x80\xa6DhLowest position where <code>k</code> consecutive set bits start, if any.DcDilates a plain integer. Bits above <code>width</code> are dropped.AmUndoes <code>from_int</code>.B`<code>0x80</code> in every lane.B`<code>0x01</code> in every lane.BhPosition of the highest set bit, if any.AbThe lowest 8 bits.DhMask with the <code>n</code> lowest bits set, <code>n &lt;= BITS</code>.BfNumber of set bits in the whole slice.CkPer-limb prefix XOR, with the parity of all lower limbs \xe2\x80\xa6EhPrefix XOR: bit <code>i</code> of the result is the parity of bits <code>0..=i</code>\xe2\x80\xa6CiIterator over the positions of set bits, lowest first \xe2\x80\xa6ElInverse of <code>prefix_xor</code>: <code>x ^ (x &lt;&lt; 1)</code>, the positions where \xe2\x80\xa6CbMirror of <code>fill_up</code>, stepping downward.BgPosition of the lowest set bit, if any.CjWraps an already-dilated bit pattern. Bits outside the \xe2\x80\xa6AgWraps an existing code.Ck<code>\xe2\x8c\x88log\xe2\x82\x82 self\xe2\x8c\x89</code>; <code>None</code> for zero.DaPositions of the set bits, ascending. See <code>Positions</code>.EoBit <code>i</code> of the result is the OR of bits <code>0..=i</code> of <code>self</code>: \xe2\x80\xa6CnBit-loop reference semantics. Slow, obviously correct, the \xe2\x80\xa6EeSets (<code>true</code>) or clears every cell of the <code>w \xc3\x97 h</code> block at \xe2\x80\xa6BjEvery bit at or below the highest set bit.DkSum of all lanes; lanes must each be <code>&lt; 256 / lanes</code> to avoidDgTranspose about the main diagonal: bit <code>(r, c)</code> moves to \xe2\x80\xa6AlNumber of set bits (POPCNT).EaSwaps the bits selected by <code>mask</code> with the bits <code>shift</code> \xe2\x80\xa6EcFirst-fit (row-major) position <code>(row, col)</code> of a <code>w \xc3\x97 h</code> \xe2\x80\xa6BeWraps limbs, least significant first.Ck<code>\xe2\x8c\x8alog\xe2\x82\x82 self\xe2\x8c\x8b</code>; <code>None</code> for zero.E`Bit <code>i</code> of the result is the XOR (parity) of bits <code>0..=i</code>.CgBit <code>i</code> = parity of bits <code>0..=i</code>.FdNumber of set bits at positions <code>&lt; i</code>; <code>i</code> must be <code>&lt;= BITS</code>.EdRotate 180\xc2\xb0: <code>(r, c)</code> \xe2\x86\x92 <code>(7 \xe2\x88\x92 r, 7 \xe2\x88\x92 c)</code>. Bit reversal.FcBit <code>p</code> of the result is set iff bits <code>p..p + k</code> of <code>self</code> are \xe2\x80\xa6EdBit <code>p</code> set iff bits <code>p..p + k</code> of <code>x</code> are all set.CeThe byte <code>b</code> repeated in every 8-bit lane.CiHigh bit of every lane whose byte is zero; other bits \xe2\x80\xa6DjInverse of <code>gray_encode</code>: the prefix XOR from the top, i.e. \xe2\x80\xa6DlReflected binary Gray code: <code>x ^ (x &gt;&gt; 1)</code>. Consecutive \xe2\x80\xa6CmLength of the longest run of set bits (Hacker\xe2\x80\x99s Delight \xe2\x80\xa6FcWrites into <code>out[r]</code> the columns <code>c</code> at which a <code>w \xc3\x97 h</code> block \xe2\x80\xa6ChPositions immediately following an odd-length run of \xe2\x80\xa6CeNumber of consecutive set bits from the top downward.DhSmallest edit distance between <code>pattern</code> and any substring ofCkRotate 90\xc2\xb0 clockwise (rows upward, columns rightward): \xe2\x80\xa6Ce<code>self + other</code> in dilated space, wrapping.BcThe carry chain: wrapping addition.CeSchoolbook multiplication modulo <code>2^BITS</code>.CmWrapping multiplication: the carry chain applied to every \xe2\x80\xa6BgThe borrow chain: wrapping subtraction.FbLevenshtein distance between <code>pattern</code> and <code>text</code>, or <code>None</code> whenEfReverse the row order: <code>(r, c)</code> \xe2\x86\x92 <code>(7 \xe2\x88\x92 r, c)</code>. One byte \xe2\x80\xa6Bh<code>true</code> when any lane is zero.DaNumber of leading zero bits; <code>BITS</code> when zero (LZCNT).EaRotate 90\xc2\xb0 counter-clockwise: <code>(r, c)</code> \xe2\x86\x92 <code>(c, 7 \xe2\x88\x92 r)</code>.FfSmallest power of two <code>&gt;= self</code> (<code>1</code> for zero); <code>None</code> when it \xe2\x80\xa6FeSmallest power of two <code>&gt;= x</code>, <code>1</code> for zero, <code>None</code> if it does \xe2\x80\xa6EiPosition of the <code>k</code>-th set bit. Precondition <code>k &lt; popcount</code>; \xe2\x80\xa6CaNumber of consecutive set bits from bit 0 upward.BhNumber of lanes equal to <code>b</code>.CmGray code: decode undoes encode, consecutive codes differ \xe2\x80\xa6CaLowest set position <code>&gt;= i</code>, if any.DaIndex of the lowest set bit; <code>BITS</code> when zero (TZCNT).Eg<code>expand</code> composes dually to <code>compact</code> (doc 13 \xc2\xa79.1 folklore, \xe2\x80\xa6BfIndex of the lowest zero lane, if any.DiOnly the lowest set bit (BLSI): <code>x &amp; -x</code>; zero stays zero.DiLargest power of two <code>&lt;= self</code>; <code>None</code> for zero.DdSets the lowest clear bit: <code>x | (x + 1)</code>; all-ones stays.DoEvery bit strictly below the lowest set bit: <code>!x &amp; (x \xe2\x88\x92 1)</code>\xe2\x80\xa6CbClears the lowest run of consecutive set bits: \xe2\x80\xa6CcClears the lowest set bit (BLSR); identity on zero.EhComposition: extracting under <code>n</code> from an extract under <code>m</code> is \xe2\x80\xa6CdEncoding then decoding a Morton code returns the \xe2\x80\xa6CfEvery bit at or below the lowest set bit (BLSMSK): \xe2\x80\xa6Dj<code>block_starts</code> agrees with the per-cell definition, reduces \xe2\x80\xa6CiDelta swaps with a common shift and pairwise disjoint \xe2\x80\xa6ClDilating then un-dilating is the identity on values that \xe2\x80\xa6EfReverse the column order: <code>(r, c)</code> \xe2\x86\x92 <code>(r, 7 \xe2\x88\x92 c)</code>. Three \xe2\x80\xa6Ec<code>wrapping_add</code> in dilated space is <code>+</code> modulo the dilated \xe2\x80\xa6CnGosper\xe2\x80\x99s hack (HAKMEM 175): the next larger integer with \xe2\x80\xa6EgSmallest <code>y &gt; x</code> with <code>count_ones(y) == count_ones(x)</code>, by \xe2\x80\xa6FcStarts of <code>k + 1</code>-runs are a subset of starts of <code>k</code>-runs. <code>k</code> \xe2\x80\xa6DhPosition of the <code>k</code>-th set bit of a 64-bit word without PDEP.DgBit <code>i</code> = parity of bits <code>i..BITS</code> (Gray decode).DjRun detection composes additively: detecting <code>b</code>-runs among \xe2\x80\xa6CmKogge\xe2\x80\x93Stone fills agree with iterated single steps, and \xe2\x80\xa6ChThe lowest-bit family agrees with the position-based \xe2\x80\xa6Ei<code>rank_below</code> is monotone in its bound. <code>i</code> in <code>0..BITS</code>.Fe<code>select(k)</code> lands on a set bit whose rank is <code>k</code>; <code>None</code> exactly \xe2\x80\xa6AkThe XOR-linear combinators.E`<code>incr</code> in dilated space is <code>+ 1</code> modulo the dilated width.Di<code>expand \xe2\x88\x98 compact</code> under the same mask keeps exactly the \xe2\x80\xa6Di<code>delta_swap</code> is an involution and moves exactly the masked \xe2\x80\xa6Di<code>compact \xe2\x88\x98 expand</code> under the same mask keeps exactly the \xe2\x80\xa6Cg<code>expand</code> agrees with the per-bit definition.Ej<code>run_starts</code> preserves meets: a window is all-set in <code>x &amp; y</code> \xe2\x80\xa6Eo<code>select</code> agrees with the per-bit definition. <code>k</code> in <code>0..BITS</code>.Ch<code>compact</code> agrees with the per-bit definition.G`Slice <code>rank</code>/<code>select</code>/<code>next_set_after</code>/<code>find_run</code> agree with the \xe2\x80\xa6Cj<code>compact</code> preserves the count of selected bits.Bk<code>run_starts(1)</code> is the identity.CfEvery SWAR lane predicate agrees with the per-byte \xe2\x80\xa6CeFills are idempotent and extensive (contain their \xe2\x80\xa6Dg<code>first_set</code> and <code>last_set</code> bracket every set bit.Ea<code>step_x</code> / <code>step_y</code> move one cell along the axis, wrapping.Bn<code>rank_below(BITS)</code> is the popcount.Dh<code>positions()</code> enumerates exactly the set bits, ascending, andCbPower-of-two helpers agree with their definitions.Ck<code>prefix_xor</code> agrees with the per-bit definition.Ea<code>run_starts</code> agrees with the per-bit definition. <code>k</code> in \xe2\x80\xa6CkThe OR-to-AND (De Morgan-shaped) combinators: a lane is \xe2\x80\xa6Eo<code>longest_run</code> is the largest <code>k</code> with a run, and <code>0</code> iff zero.Ed<code>parity</code> is the low bit of the popcount; <code>trailing_ones</code> / \xe2\x80\xa6Dh<code>find_escaped</code> over a chain of words agrees with a bit-by-bitBhThe 8\xc3\x978 board permutations are correct.ClGray code successor: consecutive codes differ exactly in \xe2\x80\xa6BlRank and select are inverse on set bits: \xe2\x80\xa6Db<code>prefix_or</code> is all-ones from the lowest set bit upward.Cf<code>delta_xor</code> undoes <code>prefix_xor</code>.DjAligned <code>2\xc3\x972</code> blocks are contiguous code ranges: the block \xe2\x80\xa6Cf<code>prefix_xor</code> undoes <code>delta_xor</code>.Bk<code>decr</code> undoes <code>incr</code>.ClGosper\xe2\x80\x99s hack yields the smallest larger word with the \xe2\x80\xa6")