rd_("DgThe <code>x</code> coordinate, still dilated (for arithmetic in place).BmThe <code>y</code> coordinate, still dilated.kBitwise OR.0BhThe eight directions, the queen\xe2\x80\x99s set.BaA sliding direction on the board.CnThe instruction set the lanes are in. A value of a carrier \xe2\x80\xa6ClA set of CPU features, as a zero-sized value that proves \xe2\x80\xa6D`<code>x \xe2\x86\xa6 !x</code>: the identity with every constant bit set.oOnly bit 0 set.AbThe constant term.BnWrapping add per lane; no carry leaves a lane.ClHacker\xe2\x80\x99s Delight 2-18: add the low seven bits of every \xe2\x80\xa6lBitwise AND.0BbWhether bit <code>i</code> is set.Ck<code>true</code> when bit <code>i &lt; BITS</code> is set.AiWhere it ends, exclusive.CiInstruction sets as values: pick one where a hot loop \xe2\x80\xa6BjThe token of the lanes\xe2\x80\x99 instruction set.BhThe raw key, the word to sort and store.lThe raw key.oLength in bits.AjUnsigned maximum per lane.AjUnsigned minimum per lane.EgA map from its matrix in <code>gf2p8affineqb</code> layout (byte <code>7 - i</code> \xe2\x80\xa6CfLanes from the word\xe2\x80\x99s bytes, lane 0 in the low byte.BgRank and select over <code>bits</code>.lBitwise NOT.0CgThe row that builds output bit <code>bit &lt; 8</code>.Da<code>f</code>, compiled with this token\xe2\x80\x99s target features. \xe2\x80\xa6ClSet view: a word as a set of positions <code>0..BITS</code>.Fm<code>x \xe2\x86\xa6 x &lt;&lt; n</code>; <code>n &gt;= 8</code> is <code>ZERO</code>. The identity matrix shifted \xe2\x80\xa6EbShift every lane left by <code>n</code>; <code>n &gt;= 8</code> clears every lane.CgA matrix with GFNI; otherwise 16-bit shifts and a mask.BoLogical shift left by <code>n &lt; BITS</code>.Fn<code>x \xe2\x86\xa6 x &gt;&gt; n</code>, logical; <code>n &gt;= 8</code> is <code>ZERO</code>. The identity matrix \xe2\x80\xa6EcShift every lane right by <code>n</code>; <code>n &gt;= 8</code> clears every lane.C`Logical shift right by <code>n &lt; BITS</code>.En<code>x \xe2\x86\xa6 (x as i8) &gt;&gt; n</code>, arithmetic: <code>shr</code> with the rows of the \xe2\x80\xa6CjArithmetic shift right per lane: the top bit fills the \xe2\x80\xa6CdWrapping subtract per lane; no borrow leaves a lane.CkHacker\xe2\x80\x99s Delight 2-18, the subtraction: set every top \xe2\x80\xa6DhThe leaves: one instruction each, as safe <code>#[target_feature]</code>lBitwise XOR.0Ch<code>Word::zip</code> with this token\xe2\x80\x99s instructions.FdThe low halves of <code>self</code> and <code>other</code> interleaved, <code>self</code> on the \xe2\x80\xa6nWidth in bits.C`Every combinator of the algebra as a method. \xe2\x80\xa6BhAlong a rank towards h: <code>+1</code>.DaThe level has GFNI: byte maps are one <code>gf2p8affineqb</code>.mAll bits set.DhEight lanes in a <code>u64</code> by SWAR: portable, and the carrier theBhAlong a rank towards a: <code>-1</code>.Ed<code>N</code> little-endian limbs of 64 bits: limb 0 holds bits <code>0..64</code>.AnThe word the slice is made of.BhThe word of the coordinates and the key.0DjA fixed-width word of <code>BITS</code> bits, bit 0 least significant: \xe2\x80\xa6Ae<code>x \xe2\x86\xa6 0</code>.oAll bits clear.CgThe one trait to import: every combinator, on every \xe2\x80\xa6AlThe raw dilated bit pattern.iThe word.B`The bits this directory indexes.nPEXT and PDEP.mThe raw code.0Dh<code>self - 1</code> in dilated space, wrapping: the borrow tunnels \xe2\x80\xa6AoReturns the argument unchanged.00000000000000000000000CkTwo-dimensional runs: rectangular blocks in a bitmap of \xe2\x80\xa6Ee<code>self + 1</code> in dilated space, wrapping at <code>width</code> bits: fill \xe2\x80\xa6BaCalls <code>U::from(self)</code>.00000000000000000000000AdLane <code>i</code>.CdThe algebra\xe2\x80\x99s laws as exported property functions.CbLanes from <code>LANES</code> bytes, lane 0 first.BaLanes from 8 bytes, lane 0 first.BbLanes from 16 bytes, lane 0 first.DfThe positions this dilation occupies: bits <code>0, D, 2D, \xe2\x80\xa6</code>.Ci<code>Word::pdep</code> with this token\xe2\x80\x99s instructions.BkPDEP by walking every position of the word.CaParallel bit deposit (PDEP): scatters the low \xe2\x80\xa6Ci<code>Word::pext</code> with this token\xe2\x80\x99s instructions.BkPEXT by walking every position of the word.DiParallel bit extract (PEXT): gathers the bits of <code>self</code> at \xe2\x80\xa6FdNumber of set bits at positions <code>&lt; i</code>; <code>i</code> must be <code>&lt;= BITS</code>.EiNumber of set bits at positions <code>&lt; i</code>. <code>i</code> may equal the bit \xe2\x80\xa6EkNumber of set bits at positions <code>&lt; i</code>; <code>i</code> past the end counts \xe2\x80\xa6AmSee <code>Rank9::rank</code>.Fc<code>x \xe2\x86\xa6 x.rotate_left(n mod 8)</code>: the rows of <code>shl</code> and of <code>shr</code> \xe2\x80\xa6BoRotate every lane left by <code>n mod 8</code>.Bk<code>x \xe2\x86\xa6 x.rotate_right(n mod 8)</code>.C`Rotate every lane right by <code>n mod 8</code>.Fd<code>self</code> first, then <code>next</code>: the composite <code>x \xe2\x86\xa6 next(self(x))</code>. \xe2\x80\xa6CcThe borrowed view, for anything not forwarded here.DhA multi-limb carrier: <code>N \xc3\x97 64</code> bits that behave as one word.ClThe carrier: a fixed-width word viewed as a container of \xe2\x80\xa6A`Every lane zero.00CkNumber of lanes, at most 64: the provided methods work in aCk<code>LANES</code> independent 8-bit lanes in one register.BeThe levels, as one value to match on.AkUp a file: <code>+8</code>.ClRank/select directory over a borrowed bit slice. See the \xe2\x80\xa6AmDown a file: <code>-8</code>.CjSixteen byte lanes with this token\xe2\x80\x99s instructions: a \xe2\x80\xa6CnSixteen lanes with the instructions the build proves: what \xe2\x80\xa6EfA slice of words as one long bit set, bit <code>i</code> of word <code>j</code> at \xe2\x80\xa6Ak<code>X86V2</code>, on x86.Cdx86-64-v2: Nehalem, Bulldozer, Silvermont and later.Ak<code>X86V3</code>, on x86.Cbx86-64-v3 and PCLMULQDQ: Haswell, Zen 1 and later.Ak<code>X86V4</code>, on x86.Cdx86-64-v4, VBMI and GFNI: Ice Lake, Zen 4 and later.ChThe map applied to one byte: eight ANDs, eight parities.FcBits of <code>other</code> where <code>mask</code> is set, bits of <code>self</code> elsewhere, \xe2\x80\xa6ElPer lane, <code>other</code> where <code>mask</code> is set and <code>self</code> elsewhere.FeFills <code>counts</code> and <code>select</code> for <code>bits</code> and returns the directory \xe2\x80\xa6DkThe keys of the rectangle as at most <code>out.len()</code> ranges: the \xe2\x80\xa6FaThe codes of the rectangle <code>x</code> \xc3\x97 <code>y</code> as at most <code>out.len()</code> \xe2\x80\xa6F`The keys of the rectangle <code>x</code> \xc3\x97 <code>y</code> as at most <code>out.len()</code> \xe2\x80\xa6CmThe curves behind one interface, for code that should not \xe2\x80\xa6nThe raw index.0CiByte lanes: the SIMD half of the algebra, on stable Rust.BcThe limbs, least significant first.CnTable lookup with PSHUFB\xe2\x80\x99s contract: a lane with its top \xe2\x80\xa6ClOne lane at a time: the portable definition, correct and \xe2\x80\xa6CfBit-parallel edit distance (Myers 1999, Hyyr\xc3\xb6\xe2\x80\x99s \xe2\x80\xa6C`<code>start..end</code>, to index the text with.EfNumber of clear bits at positions <code>&lt; i</code>; <code>i</code> past the end \xe2\x80\xa6AnSee <code>Rank9::rank0</code>.ClRank and select in O(1) over a slice of words: Vigna\xe2\x80\x99s \xe2\x80\xa6DhOne step of every bit of <code>x</code> in this direction; bits that \xe2\x80\xa6ClBit-set operations over a slice of words: the index-free \xe2\x80\xa6DkSquares attacked by the sliding pieces <code>pieces</code> in direction \xe2\x80\xa6Am<code>b</code> in every lane.0Ck<code>b</code> in every lane. No token: SWAR runs anywhere.BhWhere the occurrence starts in the text.Cj<code>Word::unzip</code> with this token\xe2\x80\x99s instructions.CmThe even bits and the odd bits, each compacted to the low \xe2\x80\xa6CmNumber of integer bits that fit: <code>ceil(BITS / D)</code>.DjA key of a 2D curve over the full width of a word: <code>LEVELS</code> \xe2\x80\xa6DjA key of a 3D curve over the full width of a word: <code>LEVELS</code> \xe2\x80\xa6BoLevels of the curve: half the bits of the word.CeLevels of the curve: a third of the bits of the word.AnLevels: <code>BITS / 2</code>.AnLevels: <code>BITS / 3</code>.CfLevels of the full-width curve: <code>BITS / 2</code>.CfLevels of the full-width curve: <code>BITS / 3</code>.Di<code>Native</code>, when the build already proves as much as the CPU \xe2\x80\xa6BnWhat the build proves, primitive by primitive.DhEvery output bit is the parity of the input: <code>0xFF</code> for a \xe2\x80\xa6BdIterator behind <code>search</code>.DjSixteen lanes as two <code>U8x8</code> halves: sound on any CPU, so it \xe2\x80\xa6CjSixteen lanes in an XMM register, at an x86 level with \xe2\x80\xa6DiAffine maps on the bits of a byte: <code>x \xe2\x86\xa6 A\xc2\xb7x \xe2\x8a\x95 b</code> over \xe2\x80\xa6EdApply an <code>Affine8</code> map to every lane: <code>gf2p8affineqb</code>, one \xe2\x80\xa6CaEight parity folds: <code>Affine8::apply8</code>.DkOne <code>gf2p8affineqb</code> with GFNI. Its immediate is the constant \xe2\x80\xa6Eg<code>x \xe2\x86\xa6 A\xc2\xb7x \xe2\x8a\x95 b</code> on one byte, bit by bit: output bit <code>i</code> is \xe2\x80\xa6CmThe map applied to every byte of a word at once: for each \xe2\x80\xa6EfAn 8\xc3\x978 bit matrix in a <code>u64</code>: bit <code>8\xc2\xb7row + col</code>. For chess \xe2\x80\xa6BoMask of lanes where <code>self == other</code>.ClMask of lanes where <code>self &gt;= other</code>, unsigned.CkMask of lanes where <code>self &gt; other</code>, unsigned.ClMask of lanes where <code>self &lt;= other</code>, unsigned.CnThe lane compare of Hacker\xe2\x80\x99s Delight 6-1 with full 8-bit \xe2\x80\xa6Cg<code>a &lt;= b</code> iff <code>min(a, b) == a</code>.CkMask of lanes where <code>self &lt; other</code>, unsigned.BdThe <code>(x, y)</code> of this key.BgThe <code>(x, y, z)</code> of this key.EhSplits the code back into <code>(x, y)</code>: <code>Word::unzip</code>, two PEXT of \xe2\x80\xa6CaSplits the code back into <code>(x, y, z)</code>.CnThe <code>(x, y)</code> of this index on the full-width curve.DaThe <code>(x, y, z)</code> of this index on the full-width curve.B`The level a dispatch should run.CnThe token, if the CPU and the OS have every feature of the \xe2\x80\xa600EaThe key of <code>(x, y)</code>; coordinate bits above <code>LEVELS</code> are \xe2\x80\xa6EdThe key of <code>(x, y, z)</code>; coordinate bits above <code>LEVELS</code> are \xe2\x80\xa6FeInterleaves <code>x</code> (even bits) and <code>y</code> (odd bits): <code>Word::zip</code>, two \xe2\x80\xa6DeInterleaves three coordinates. Bits above <code>LEVELS</code> are \xe2\x80\xa6DiThe index of <code>(x, y)</code> on the full-width curve. Coordinates \xe2\x80\xa6DhThe index of <code>(x, y, z)</code> on the full-width curve. CoordinatesEgScatters the low <code>count_ones(mask)</code> bits of <code>self</code> to the set \xe2\x80\xa6DbThe bits of <code>self &amp; mask</code>, in order, as the low \xe2\x80\xa6ChSpace-filling curves, rank/select, bit-parallel edit \xe2\x80\xa6CnThe two words, lanes <code>0..8</code> and <code>8..16</code>.0BjThe matrix, in the instruction\xe2\x80\x99s layout.Cd<code>true</code> when an odd number of bits is set.DeApproximate occurrences of <code>pattern</code> in <code>text</code>.EgPosition of the <code>k</code>-th set bit (0-indexed), if <code>k &lt; popcount</code>.EfPosition of the <code>k</code>-th set bit (from 0), if <code>k &lt; count_ones</code>.Ck<code>Bits::select</code> with this token\xe2\x80\x99s instructions.CgPosition of the <code>k</code>-th set bit, by counting.EkPosition of set bit number <code>k</code> (0-based), if <code>k &lt; count_ones()</code>\xe2\x80\xa6AoSee <code>Rank9::select</code>.BnThe code of <code>(x + 1, y)</code>, wrapping.BnThe code of <code>(x, y + 1)</code>, wrapping.CmThe nibble tables that compute the map by linearity: with \xe2\x80\xa6AmUndoes <code>from_int</code>.BgReplaces the <code>x</code> coordinate.BgReplaces the <code>y</code> coordinate.Ck<code>zip</code> with the instructions of <code>isa</code>.DkAn affine map on the bits of a byte: <code>x \xe2\x86\xa6 A\xc2\xb7x \xe2\x8a\x95 b</code> over \xe2\x80\xa6ClThe word <code>to_bitmask</code> produces: one bit per lane.EhAn integer whose bits sit at positions <code>0, D, 2D, \xe2\x80\xa6</code> of <code>W</code>. \xe2\x80\xa6EeA 2D Morton code: <code>x</code> dilated by 2 in the even bits, <code>y</code> in \xe2\x80\xa6FbA 3D Morton code: <code>x</code> dilated by 3 in bits <code>0, 3, 6, \xe2\x80\xa6</code>, <code>y</code> inDc<code>x \xe2\x86\xa6 x.reverse_bits()</code>: the identity with its rows \xe2\x80\xa6CmIterator over the subsets of a mask, ascending, from zero \xe2\x80\xa6CfSaturating add per lane: <code>min(x + y, 255)</code>.EhGathers the bits of <code>self</code> at the set positions of <code>mask</code> into \xe2\x80\xa6Ee<code>self \xe2\x88\x98 first</code>: <code>first</code> is applied first. The same map as \xe2\x80\xa6BlDilated integers and Morton (Z-order) codes.DhCells reachable from the set bits of <code>self</code> by repeatedly \xe2\x80\xa6Em<code>true</code> iff <code>self</code> contains at least one run of <code>k</code> set bits.CnHilbert curves as two scans over the Morton code: parities \xe2\x80\xa6Bj<code>true</code> for exactly one set bit.Be<code>true</code> when no bit is set.Cl<code>pdep</code> with the instructions of <code>isa</code>.BdPermutations built from delta swaps.Fc<code>pext</code> with the instructions of <code>isa</code>. The seven <code>_in</code> methods \xe2\x80\xa6CfEverything most code needs, one <code>use</code> away.C`<code>rank</code> compiled for <code>isa</code>.Ec<code>rank</code> compiled for <code>isa</code>: POPCNT on a level that has it, \xe2\x80\xa6AhSets bit <code>i</code>.DkByte permute by data, PSHUFB\xe2\x80\x99s contract with <code>self</code> as the \xe2\x80\xa6CiSaturating subtract per lane: <code>max(x - y, 0)</code>.DgEvery subset of <code>self</code> as a mask, ascending, starting at \xe2\x80\xa6CmAny Boolean function of three words, bit by bit, from its \xe2\x80\xa6CmAny Boolean function of three registers, bit by bit, from \xe2\x80\xa6EoBit <code>i</code> of the result is bit <code>4 a_i + 2 b_i + c_i</code> of <code>table</code>.CdThe <code>target_feature</code> string of the level.00EcA 2D Hilbert index over the full width of <code>W</code>: <code>BITS / 2</code> \xe2\x80\xa6EeA 3D Hilbert index over <code>BITS / 3</code> levels of <code>W</code>: a cube of \xe2\x80\xa6Ae<code>x \xe2\x86\xa6 x</code>.Af<code>Portable</code>.CcNo features: the broadword definitions, everywhere.Dj<code>Rank9</code> with the directory allocated for you: one call from \xe2\x80\xa6DgThe x86 levels with SSSE3, which is what <code>X86x16</code> needs. \xe2\x80\xa6BoHigh bit of every lane equal to <code>b</code>.FhHigh bit of every lane whose byte is <code>&gt;= n</code>, for <code>n</code> in <code>1..=128</code>\xe2\x80\xa6FeHigh bit of every lane whose byte is <code>&lt; n</code>, for <code>n</code> in <code>1..=128</code>.CmHow the shipped kernels were composed, so you can compose \xe2\x80\xa6CkDetects the level once (or takes one) and runs the body \xe2\x80\xa6Dg<code>distance_in</code> with the carrier chosen by the pattern\xe2\x80\x99s \xe2\x80\xa6ClEdits between <code>text[start..end]</code> and the pattern.DhLowest position where <code>k</code> consecutive set bits start, if any.DcDilates a plain integer. Bits above <code>width</code> are dropped.A`Wraps a raw key.0Cn3D Hilbert curves: the decode as a Kogge\xe2\x80\x93Stone scan over \xe2\x80\xa6C`<code>true</code> when there are no bits at all.B`<code>0x80</code> in every lane.B`<code>0x01</code> in every lane.BhPosition of the highest set bit, if any.AbThe lowest 8 bits.DhMask with the <code>n</code> lowest bits set, <code>n &lt;= BITS</code>.Cf<code>Word::pdep</code> on <code>u32</code>: one PDEP.Cf<code>Word::pdep</code> on <code>u64</code>: one PDEP.Cf<code>Word::pext</code> on <code>u32</code>: one PEXT.Cf<code>Word::pext</code> on <code>u64</code>: one PEXT.Cm<code>unzip</code> with the instructions of <code>isa</code>.Cm<code>Word::xor_scan</code> with this token\xe2\x80\x99s instructions.EhPrefix XOR: bit <code>i</code> of the result is the parity of bits <code>0..=i</code>\xe2\x80\xa6A`<code>+9</code>.A`<code>+7</code>.CiIterator over the positions of set bits, lowest first \xe2\x80\xa6A`<code>-7</code>.A`<code>-9</code>.DjEvery level this CPU has, lowest first, and <code>Level::Native</code> \xe2\x80\xa6Dj<code>(x + y) / 2</code> per lane, rounding down (<code>uhadd</code>): \xe2\x80\xa6Ed<code>(x + y + 1) / 2</code> per lane, no overflow (PAVGB, <code>urhadd</code>): \xe2\x80\xa6AjClears bit <code>i</code>.ElInverse of <code>prefix_xor</code>: <code>x ^ (x &lt;&lt; 1)</code>, the positions where \xe2\x80\xa6CbMirror of <code>fill_up</code>, stepping downward.BgPosition of the lowest set bit, if any.CjWraps an already-dilated bit pattern. Bits outside the \xe2\x80\xa6AgWraps an existing code.0DjA map from its rows in bit order: <code>rows[i]</code> masks the input \xe2\x80\xa6Dg<code>true</code> when the constant is zero: the map is linear, and \xe2\x80\xa6Ck<code>\xe2\x8c\x88log\xe2\x82\x82 self\xe2\x8c\x89</code>; <code>None</code> for zero.CjCarry-less multiplication as a scan. Not on soft-float \xe2\x80\xa6DaPositions of the set bits, ascending. See <code>Positions</code>.ClPositions of every set bit, ascending, across the words: \xe2\x80\xa6EoBit <code>i</code> of the result is the OR of bits <code>0..=i</code> of <code>self</code>: \xe2\x80\xa6DiRank only, at 25 % instead of 62.5 % of the bits; <code>select</code> \xe2\x80\xa6CnBit-loop reference semantics. Slow, obviously correct, the \xe2\x80\xa6Eh<code>select</code> compiled for <code>isa</code>: POPCNT to find the word, PDEP and \xe2\x80\xa6Eh<code>select</code> compiled for <code>isa</code>: POPCNT in the SWAR compares, PDEP \xe2\x80\xa6BjEvery bit at or below the highest set bit.DkSum of all lanes; lanes must each be <code>&lt; 256 / lanes</code> to avoidCmTo the Morton code of the same cell: the levels read back \xe2\x80\xa6CkTo the Morton code of the same cell: the frames as a suffixDgTranspose about the main diagonal: bit <code>(r, c)</code> moves to \xe2\x80\xa6AoInterleave the high halves: \xe2\x80\xa6AnInterleave the low halves: \xe2\x80\xa6EfOne approximate occurrence: <code>text[start..end]</code> is <code>distance</code> \xe2\x80\xa6BfNumber of set bits in the whole slice.AcNumber of set bits.BcSee <code>Rank9::count_ones</code>.AlNumber of set bits (POPCNT).F`Words of <code>counts</code> that <code>build</code> needs for <code>words</code> words of bits.EaSwaps the bits selected by <code>mask</code> with the bits <code>shift</code> \xe2\x80\xa6EdSets every cell of the <code>w \xc3\x97 h</code> block at <code>(row, col)</code>. The \xe2\x80\xa6EcFirst-fit (row-major) position <code>(row, col)</code> of a <code>w \xc3\x97 h</code> \xe2\x80\xa6oWraps an index.0BeWraps limbs, least significant first.FeThe directory over <code>counts</code> and <code>select</code> that <code>build</code> filled for \xe2\x80\xa6Cf<code>true</code> when the select inventory was built.DhWhether some cell of the rectangle has its key in <code>keys</code>. SeeEgWhether some cell of the rectangle <code>x</code> \xc3\x97 <code>y</code> has its code in \xe2\x80\xa6EhWhether some cell of the rectangle <code>x</code> \xc3\x97 <code>y</code> has its index in \xe2\x80\xa6ChThe directory\xe2\x80\x99s two buffers, to keep next to the bits.Ck<code>\xe2\x8c\x8alog\xe2\x82\x82 self\xe2\x8c\x8b</code>; <code>None</code> for zero.E`Bit <code>i</code> of the result is the XOR (parity) of bits <code>0..=i</code>.CgBit <code>i</code> = parity of bits <code>0..=i</code>.EdRotate 180\xc2\xb0: <code>(r, c)</code> \xe2\x86\x92 <code>(7 \xe2\x88\x92 r, 7 \xe2\x88\x92 c)</code>. Bit reversal.FcBit <code>p</code> of the result is set iff bits <code>p..p + k</code> of <code>self</code> are \xe2\x80\xa6EdBit <code>p</code> set iff bits <code>p..p + k</code> of <code>x</code> are all set.FdWords of <code>select</code> that <code>build</code> needs for <code>words</code> words of bits: \xe2\x80\xa6Fk<code>Word::select_lowest</code> on <code>u64</code>: <code>trailing_zeros(pdep(1 &lt;&lt; k, x))</code>\xe2\x80\xa6CeThe byte <code>b</code> repeated in every 8-bit lane.EfBit <code>i</code> of the result is the XOR (parity) of bits <code>i..BITS</code>: \xe2\x80\xa6EcOne bit per lane, bit <code>i</code> = the top bit of lane <code>i</code>. On a \xe2\x80\xa6CiHigh bit of every lane whose byte is zero; other bits \xe2\x80\xa6DhThe carrier of <code>Native</code>: SSSE3 or NEON when the build has \xe2\x80\xa6CaIterator behind <code>Search::occurrences</code>.EeClears every cell of the <code>w \xc3\x97 h</code> block at <code>(row, col)</code>, as \xe2\x80\xa6FbLevenshtein distance between <code>pattern</code> and <code>text</code>, or <code>None</code> whenFlLanes from two words: lanes <code>0..8</code> from <code>lo</code>, <code>8..16</code> from <code>hi</code>.CmFrom the Morton code of the same cell: the levels re-read \xe2\x80\xa6CkFrom the Morton code of the same cell: the twelve-state \xe2\x80\xa6DeInverse of <code>gray_encode</code>: the prefix XOR from the top, \xe2\x80\xa6DlReflected binary Gray code: <code>x ^ (x &gt;&gt; 1)</code>. Consecutive \xe2\x80\xa6CmLength of the longest run of set bits (Hacker\xe2\x80\x99s Delight \xe2\x80\xa6EgThe carry-rippler: the next subset of <code>mask</code> after <code>self</code>, in \xe2\x80\xa6ClThe occurrences behind the ends: one per run of adjacent \xe2\x80\xa6CiThe truth table of a Boolean function of three words: \xe2\x80\xa6CkPer-limb prefix XOR, with the parity of all lower limbs \xe2\x80\xa6D`<code>xor_scan</code> with the instructions of <code>isa</code>.FcWrites into <code>out[r]</code> the columns <code>c</code> at which a <code>w \xc3\x97 h</code> block \xe2\x80\xa6EdLanes <code>n..n + LANES</code> of the concatenation <code>self ++ other</code>, \xe2\x80\xa6CfPALIGNR takes an immediate; the match folds to one \xe2\x80\xa6EhThe <code>(x, y)</code> of this index on the curve of <code>order</code> levels; the \xe2\x80\xa6EgThe <code>(x, y, z)</code> of this index on the curve of <code>order</code> levels; \xe2\x80\xa6FeThe index of <code>(x, y)</code> on the curve of <code>order</code> levels, <code>order</code> in \xe2\x80\xa6FeThe index on the curve of <code>order</code> levels, <code>order</code> in <code>0..=LEVELS</code>\xe2\x80\xa6ChPositions immediately following an odd-length run of \xe2\x80\xa6CeNumber of consecutive set bits from the top downward.EgReverse the bits of every lane: <code>Affine8::REVERSE</code>; <code>rbit</code> on \xe2\x80\xa6CcReverse the word, then put its bytes back in order.EdBit <code>i</code> of the result is bit <code>BITS - 1 - i</code> of <code>x</code>.CkRotate 90\xc2\xb0 clockwise (rows upward, columns rightward): \xe2\x80\xa6FbSum over all lanes of <code>|self - other|</code> (PSADBW, <code>uabd</code> + <code>addv</code>):BhPSADBW leaves a sum in each 64-bit half.Ce<code>self + other</code> in dilated space, wrapping.BcThe carry chain: wrapping addition.CeSchoolbook multiplication modulo <code>2^BITS</code>.CmWrapping multiplication: the carry chain applied to every \xe2\x80\xa6BgThe borrow chain: wrapping subtraction.Dk<code>Word::xor_scan</code> on <code>u64</code>: one PCLMULQDQ by all ones.Ee<code>count_ones</code> compiled for <code>isa</code>: POPCNT on a level that has \xe2\x80\xa6EfReverse the row order: <code>(r, c)</code> \xe2\x86\x92 <code>(7 \xe2\x88\x92 r, c)</code>. One byte \xe2\x80\xa6EcThe factor that moves the <code>i</code>-th set bit of <code>mask</code> to bit \xe2\x80\xa6Bh<code>true</code> when any lane is zero.EgThe Hilbert index of <code>(x, y)</code> on the curve of <code>order</code> levels: \xe2\x80\xa6DaNumber of leading zero bits; <code>BITS</code> when zero (LZCNT).DgThe low nibble of every lane looked up in <code>lo</code>, the high \xe2\x80\xa6DfPMADDUBSW: for every pair of lanes <code>k</code>, the dot product \xe2\x80\xa6AeThe token, unchecked.00FbLowest set position <code>&gt;= i</code>, if any: from <code>i</code> on, <code>i</code> included.EaRotate 90\xc2\xb0 counter-clockwise: <code>(r, c)</code> \xe2\x86\x92 <code>(c, 7 \xe2\x88\x92 r)</code>.FfSmallest power of two <code>&gt;= self</code> (<code>1</code> for zero); <code>None</code> when it \xe2\x80\xa6FeSmallest power of two <code>&gt;= x</code>, <code>1</code> for zero, <code>None</code> if it does \xe2\x80\xa6EgPosition of the <code>k</code>-th set bit (from 0), or <code>BITS</code> when there \xe2\x80\xa6CaNumber of consecutive set bits from bit 0 upward.Db<code>Word::xor_scan_down</code> with this token\xe2\x80\x99s instructions.DfSuffix XOR: bit <code>i</code> of the result is the parity of bits \xe2\x80\xa6BhNumber of lanes equal to <code>b</code>.D`<code>decode</code> of a column of codes into two columns of \xe2\x80\xa60Db<code>decode</code> of a column of indices into two columns of \xe2\x80\xa60Dd<code>decode</code> of a column of indices into three columns of \xe2\x80\xa6Eh<code>encode</code> over two columns of coordinates: <code>out[i]</code> is the code \xe2\x80\xa60Ec<code>encode</code> over two columns of coordinates: <code>out[i]</code> is the \xe2\x80\xa60Dj<code>encode</code> over three columns of coordinates into a column of \xe2\x80\xa6FlLanes from two words: lanes <code>0..8</code> from <code>lo</code>, <code>8..16</code> from <code>hi</code>.CmGray code: decode undoes encode, consecutive codes differ \xe2\x80\xa6FbThe <code>(x, y)</code> of Hilbert index <code>d</code> on the curve of <code>order</code> levels:CkTwo table lookups are one: on lanes with the top bit clear,DaIndex of the lowest set bit; <code>BITS</code> when zero (TZCNT).DkComposition is matrix multiplication: <code>a.then(b)</code> applied is \xe2\x80\xa6Eg<code>expand</code> composes dually to <code>compact</code> (doc 13 \xc2\xa79.1 folklore, \xe2\x80\xa6BfIndex of the lowest zero lane, if any.DkAn order-preserving gather triple agrees with <code>Bits::compact</code>\xe2\x80\xa6DiOnly the lowest set bit (BLSI): <code>x &amp; -x</code>; zero stays zero.DiLargest power of two <code>&lt;= self</code>; <code>None</code> for zero.DdSets the lowest clear bit: <code>x | (x + 1)</code>; all-ones stays.Dk<code>suffix_xor</code> is the per-bit parity from the top, the inverse \xe2\x80\xa6Fb<code>unzip</code> undoes <code>zip</code> on the low halves, which is all <code>zip</code> reads.DoEvery bit strictly below the lowest set bit: <code>!x &amp; (x \xe2\x88\x92 1)</code>\xe2\x80\xa6CbClears the lowest run of consecutive set bits: \xe2\x80\xa6CcClears the lowest set bit (BLSR); identity on zero.EhComposition: extracting under <code>n</code> from an extract under <code>m</code> is \xe2\x80\xa6ChPortable PDEP: expand by parallel suffix (Hacker\xe2\x80\x99s \xe2\x80\xa6EhThe factor that sends the <code>i</code>-th set bit of <code>mask</code> (ascending) \xe2\x80\xa6CdEncoding then decoding a Morton code returns the \xe2\x80\xa6Eg<code>prefix_xor</code> over a stream of words: <code>carry</code> is the parity of \xe2\x80\xa6Ec<code>rank</code> is monotone in its bound. <code>i</code> in <code>0..BITS</code>.Ef<code>select_lowest</code> with the instructions of <code>isa</code>. The provided \xe2\x80\xa6CfEvery bit at or below the lowest set bit (BLSMSK): \xe2\x80\xa6De<code>xor_scan_down</code> with the instructions of <code>isa</code>.Dj<code>block_starts</code> agrees with the per-cell definition, reduces \xe2\x80\xa6CiDelta swaps with a common shift and pairwise disjoint \xe2\x80\xa6ClDilating then un-dilating is the identity on values that \xe2\x80\xa6EfCalls <code>f</code> with every set position, ascending: <code>positions</code> by \xe2\x80\xa6ClOn the full-width curve: encode undoes decode and decode \xe2\x80\xa6EfReverse the column order: <code>(r, c)</code> \xe2\x86\x92 <code>(r, 7 \xe2\x88\x92 c)</code>. Three \xe2\x80\xa6Dg<code>Ord</code> is the order of unsigned integers: the highest bit \xe2\x80\xa6Ec<code>Word::xor_scan_down</code> on <code>u64</code>: the high half of the same \xe2\x80\xa6CjPortable PEXT: compress by parallel suffix (Hacker\xe2\x80\x99s \xe2\x80\xa6Ec<code>wrapping_add</code> in dilated space is <code>+</code> modulo the dilated \xe2\x80\xa6DaA gather triple with placement <code>place</code> agrees with \xe2\x80\xa6CiEncode undoes decode on coordinates of the full-width \xe2\x80\xa6DkThe curve of <code>order + 1</code> restricted to its first quadrant is \xe2\x80\xa6CnGosper\xe2\x80\x99s hack (HAKMEM 175): the next larger integer with \xe2\x80\xa6EgSmallest <code>y &gt; x</code> with <code>count_ones(y) == count_ones(x)</code>, by \xe2\x80\xa6FcStarts of <code>k + 1</code>-runs are a subset of starts of <code>k</code>-runs. <code>k</code> \xe2\x80\xa6DhPosition of the <code>k</code>-th set bit of a 64-bit word without PDEP.DhSmallest edit distance between <code>pattern</code> and any substring ofDg<code>to_morton</code> over a slice of keys, in place: each Hilbert \xe2\x80\xa6000Ed<code>zip</code> is two expands, the second a bit up: <code>a</code> on the even \xe2\x80\xa6DiThe curve of <code>order + 1</code> restricted to its first octant is \xe2\x80\xa6DgBit <code>i</code> = parity of bits <code>i..BITS</code> (Gray decode).DkThe directory agrees with the linear scans of <code>crate::slice</code> \xe2\x80\xa6DjRun detection composes additively: detecting <code>b</code>-runs among \xe2\x80\xa6Cm<code>for_each_position</code> compiled for <code>isa</code>.Dh<code>from_morton</code> over a slice of keys, in place: each Morton \xe2\x80\xa6000DjOverflow of <code>self + other</code> read as two\xe2\x80\x99s complement, from \xe2\x80\xa6DkOverflow of <code>self \xe2\x88\x92 other</code> read as two\xe2\x80\x99s complement: the \xe2\x80\xa6CmKogge\xe2\x80\x93Stone fills agree with iterated single steps, and \xe2\x80\xa6DkThe Hilbert index of <code>(x, y)</code> on the full-width curve by the \xe2\x80\xa6CjThe lane algebra and the word algebra agree where they \xe2\x80\xa6DbEvery <code>Lanes</code> operation against its per-lane scalar \xe2\x80\xa6Bh<code>rank(BITS)</code> is the popcount.Di<code>unzip</code> is the even bits and the odd bits, each compacted: \xe2\x80\xa6ChThe lowest-bit family agrees with the position-based \xe2\x80\xa6CjThe 3D Hilbert encode as the twelve-state machine, one \xe2\x80\xa6Fe<code>select(k)</code> lands on a set bit whose rank is <code>k</code>; <code>None</code> exactly \xe2\x80\xa6Fa<code>select_lowest</code> is total: the <code>k</code>-th set bit, or <code>BITS</code> when \xe2\x80\xa6Dh<code>Bits::ternary</code> agrees with the bit-by-bit reading of the \xe2\x80\xa6AkThe XOR-linear combinators.E`<code>incr</code> in dilated space is <code>+ 1</code> modulo the dilated width.CjThe 3D Hilbert decode as the twelve-state machine, one \xe2\x80\xa6Dm<code>rank</code> is monotone and steps by exactly the bit at <code>i</code>.DoBits <code>stride</code> apart with <code>stride &gt;= k</code> gather exactly.DiAn <code>Affine8</code> map agrees with its bit-by-bit definition: on \xe2\x80\xa6Di<code>expand \xe2\x88\x98 compact</code> under the same mask keeps exactly the \xe2\x80\xa6Di<code>delta_swap</code> is an involution and moves exactly the masked \xe2\x80\xa6Di<code>compact \xe2\x88\x98 expand</code> under the same mask keeps exactly the \xe2\x80\xa6Cg<code>expand</code> agrees with the per-bit definition.Ej<code>run_starts</code> preserves meets: a window is all-set in <code>x &amp; y</code> \xe2\x80\xa6Eo<code>select</code> agrees with the per-bit definition. <code>k</code> in <code>0..BITS</code>.Ea<code>to_morton_in_place</code> on the curve of <code>order</code> levels, as \xe2\x80\xa6000CmThe sixteen-lane carrier, whatever it compiles to, is two \xe2\x80\xa6Ch<code>compact</code> agrees with the per-bit definition.CjThe state-machine encode and the two-scan decode agree \xe2\x80\xa6DhThe byte maps of <code>Lanes</code> against their scalar definitions, onFd<code>select</code> inverts <code>rank</code> on set bits: for <code>k &lt; count_ones()</code>, \xe2\x80\xa6FoSlice <code>rank</code>/<code>select</code>/<code>next_set_from</code>/<code>find_run</code> agree with the \xe2\x80\xa6Cj<code>compact</code> preserves the count of selected bits.Ec<code>from_morton_in_place</code> on the curve of <code>order</code> levels, as \xe2\x80\xa6000CmThe 3D decode scan and the memoised encode agree with the \xe2\x80\xa6DhThe cover on the full <code>u64</code> grid: well formed, within budget,Bk<code>run_starts(1)</code> is the identity.CfEvery SWAR lane predicate agrees with the per-byte \xe2\x80\xa6CiThe named maps are the byte operations they are named \xe2\x80\xa6CeFills are idempotent and extensive (contain their \xe2\x80\xa6Dg<code>first_set</code> and <code>last_set</code> bracket every set bit.5DgThe cover of a rectangle on the <code>u16</code> grid (256 \xc3\x97 256), \xe2\x80\xa6Ea<code>step_x</code> / <code>step_y</code> move one cell along the axis, wrapping.Ee<code>Bits::subsets</code> enumerates every subset of <code>mask</code> exactly once,2Dh<code>positions()</code> enumerates exactly the set bits, ascending, andCbPower-of-two helpers agree with their definitions.Ck<code>prefix_xor</code> agrees with the per-bit definition.Ea<code>run_starts</code> agrees with the per-bit definition. <code>k</code> in \xe2\x80\xa6CkThe OR-to-AND (De Morgan-shaped) combinators: a lane is \xe2\x80\xa6Di<code>board8::slide</code> equals a walk from every piece, one square \xe2\x80\xa6Eo<code>longest_run</code> is the largest <code>k</code> with a run, and <code>0</code> iff zero.Ed<code>parity</code> is the low bit of the popcount; <code>trailing_ones</code> / \xe2\x80\xa6Dh<code>find_escaped</code> over a chain of words agrees with a bit-by-bitG`<code>set_bit</code> and <code>clear_bit</code> change bit <code>i</code> and no other: <code>scratch</code> \xe2\x80\xa6BnThe table of a function is the function at \xe2\x80\xa6BhThe 8\xc3\x978 board permutations are correct.ClGray code successor: consecutive codes differ exactly in \xe2\x80\xa6BlRank and select are inverse on set bits: \xe2\x80\xa6CjThe 3D column conversions are the per-point ones, both \xe2\x80\xa6EeThe curve is a path: indices <code>h</code> and <code>h + 1</code> are cells one stepDk<code>intersects</code> against the cells, on any carrier: true exactly \xe2\x80\xa6DkThe carry-rippler is <code>+ 1</code> in the compacted domain: the next \xe2\x80\xa61EgThe 3D curve is a path: indices <code>h</code> and <code>h + 1</code> are cells one \xe2\x80\xa6Db<code>prefix_or</code> is all-ones from the lowest set bit upward.CmSigned overflow from three sign bits agrees with the sign \xe2\x80\xa6Cf<code>delta_xor</code> undoes <code>prefix_xor</code>.DjAligned <code>2\xc3\x972</code> blocks are contiguous code ranges: the block \xe2\x80\xa6Cf<code>prefix_xor</code> undoes <code>delta_xor</code>.De<code>prefix_xor_carry</code> over a chain of words is the prefix \xe2\x80\xa6Bk<code>decr</code> undoes <code>incr</code>.ClThe 2D Morton column conversions are the per-point ones, \xe2\x80\xa60CmThe 2D Hilbert column conversions are the per-point ones, \xe2\x80\xa60E`<code>intersects</code> against <code>cover</code>, on any carrier and any grid.ClGosper\xe2\x80\x99s hack yields the smallest larger word with the \xe2\x80\xa6CcThe batch conversion is the per-key one, both ways.02CfThe 3D batch conversion is the per-key one, both ways.0CjThe batch on the curve of <code>order</code> levels is \xe2\x80\xa60CmThe 3D batch on the curve of <code>order</code> levels is \xe2\x80\xa60")