name: x86_64
on:
  push:
    branches: [main]
  pull_request:

# The matrix lives in the flake: `plan` reads the check names, `check` expands them.
jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      checks: ${{ steps.plan.outputs.checks }}
    steps:
      - uses: actions/checkout@v7
      - uses: DeterminateSystems/nix-installer-action@v23
      - id: plan
        run: echo "checks=$(nix eval --json .#checks.x86_64-linux --apply builtins.attrNames)" >> "$GITHUB_OUTPUT"

  check:
    needs: plan
    name: ${{ matrix.check }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        check: ${{ fromJSON(needs.plan.outputs.checks) }}
    steps:
      - uses: actions/checkout@v7
      - uses: DeterminateSystems/nix-installer-action@v23
      - uses: cachix/cachix-action@v17
        with:
          name: hakmem
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - run: nix build -L .#checks.x86_64-linux.${{ matrix.check }}

  miri:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: DeterminateSystems/nix-installer-action@v23
      - uses: cachix/cachix-action@v17
        with:
          name: hakmem
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - run: nix run .#miri-hakmem

  # Result of every cell to gh-pages for the README matrix and the site.
  # Pushes to main only: a PR run must not overwrite the status of main.
  status:
    needs: [plan, check, miri]
    if: always() && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/status
        with:
          system: x86_64-linux
